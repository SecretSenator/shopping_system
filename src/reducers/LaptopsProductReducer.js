import {LaptopData} from '../data/data'

const initialState=LaptopData;

const LaptopsProductReducer=(state=initialState,action)=>{
    switch(action.type){
        
        default: return state;
    }
}

export default LaptopsProductReducer;